'use strict'

angular.module('lunchRoom')
    .controller('MainController', ['$scope', '$document', function ($scope, $document) {

    //$scope.message = '';

    //$document.on('keypress', function (event) {
        //$scope.message = $scope.message + ' ' + event.which;
        //$scope.$digest();
    //});
}]);
